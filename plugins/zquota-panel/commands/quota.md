---
description: Show coding-plan quota (balance | preview | reset)
---
Run `node bin/zagent-quota balance`, `node bin/zagent-quota preview`, or `node bin/zagent-quota reset` from the
zagent repository root and show the JSON result as a compact table. Argument: which view
(balance default).
