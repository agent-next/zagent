---
description: Show coding-plan quota (balance | preview | reset)
---
Run `zagent quota balance`, `zagent quota preview`, or `zagent quota reset` and show
the JSON result as a compact table. Argument: which view (balance default).
